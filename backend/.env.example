# Copy this file to .env and fill in real values. Never commit .env itself.

PORT=4000
CORS_ORIGIN=*

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=fire_report_db

JWT_SECRET=change_this_to_a_long_random_string
